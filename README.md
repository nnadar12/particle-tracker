# HOW TO USE
1. clone this repository
2. run `pip install -r src/requirements.txt`
3. add desired .tif files to the data folder
4. run `python src/tracker.py`
5. select desired tiff file using terminal menu
6. output will be produced in a new run folder in the output directory
